import styled from 'styled-components';

const styles = {
  Container: styled.div`
    width: 100vw;
    height: 100vh;
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  `,
};

export default styles;
