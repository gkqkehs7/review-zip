import styled from 'styled-components';

const styles = {
  Container: styled.div`
    display: flex;
    position: relative;
    width: 100%;
    height: 100%;
    background-color: white;
  `,
};

export default styles;
